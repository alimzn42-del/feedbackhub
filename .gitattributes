# The repository stores LF and checks out LF everywhere, so a Windows and a
# Linux checkout produce byte-identical files. Matches .editorconfig.
* text=auto eol=lf

# Never munge these.
*.png binary
*.ico binary
*.jpg binary
